# 📖 dlfc-code 代码阅读指南

> 先读这份指南（10 分钟），再看任意一章的代码，会顺畅很多。
> 全书 20 章脚本共享同一套写法，认准下面 6 个套路即可。

## 1. 每章脚本的开头三段（样板）

```python
import os, sys
import numpy as np
try:
    sys.stdout.reconfigure(encoding="utf-8")   # 强制中文能打印到任何终端
except Exception:
    pass
_REPO_ROOT = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
sys.path.insert(0, _REPO_ROOT)                 # 让脚本能 import 仓库根目录的 utils.py
from utils import Figure, ...                  # 导入共享工具
```

**逐行解释**：
- `os.path.abspath(__file__)`：本脚本的绝对路径（如 `.../chapters/ch01_xxx/ch01.py`）
- `os.path.dirname`：往上一层。脚本在 `repo/chapters/chXX/` 下，往上 **3 层**才到仓库根
- `sys.path.insert(0, ...)`：把仓库根目录加入"模块搜索路径"，这样 `from utils import ...` 才找得到

## 2. section() 函数：章节分隔线

```python
def section(title):    # 打印一行 "===== 标题 ====="
    print("=" * 68)    # "=" 重复 68 次，只是装饰
    print(title)
    print("=" * 68)
```

## 3. Figure 绘图类（utils.py）：统一画图入口

```python
fig = Figure("图标题", "x轴名", "y轴名")   # 建画布
fig.line(x, y, label="曲线名")              # 画一条线
fig.scatter(x, y, label="点名")             # 画散点
fig.save("_plots/xxx.png")                  # 存图（有 matplotlib 用真图，没有自动降级画 SVG）
```
> 你不需要关心它内部怎么画——把它当"画笔"用就行。

## 4. numpy 向量化：没有 for 循环，一次算整批

全书代码几乎不用循环，而是一次性算所有样本。三个最常用的写法：

| 写法 | 意思 | 等价循环 |
|---|---|---|
| `X @ w` | 矩阵乘法：每行样本和权重做点积 | `for n: y[n] = sum(X[n]*w)` |
| `(X - mu) ** 2` | 每个元素都减 mu、都平方 | `for n,i: d[n,i] = (X[n,i]-mu[i])**2` |
| `np.mean(...)` / `np.sum(...)` | 平均 / 求和（axis= 指定沿哪个方向） | 循环累加 |

**判断形状的小技巧**：代码注释里常写 `# (N, D) @ (D, H) -> (N, H)`，
意思是"左边是 N×D 矩阵乘 D×H 矩阵，得到 N×H"——看到这样的注释就是告诉你形状怎么变的。

## 5. 数学记号 → 代码的对照

| 书中记号 | 代码里的样子 |
|---|---|
| Σ (求和) | `np.sum(...)` 或矩阵乘法 |
| wᵀx (点积) | `w @ x` |
| ΦᵀΦ (矩阵转置乘) | `Phi.T @ Phi` |
| N(x|μ,σ²) 高斯密度 | `gaussian_pdf(x, mu, sigma2)` |
| E(w) 误差函数 | 代码里常常直接叫 `loss` / `E` |
| 梯度 ∇E | 代码里 `dW`、`db`、`grad` |

## 6. 每章脚本的"验证段"

每章最后都有 `assert ...`：如果条件不成立会当场报错。
**assert 通过 = 代码与数学一致**。你运行脚本时看到 `✓` 和没有任何报错，就说明这章实现是对的。

## 推荐阅读顺序

1. `utils.py`（共享工具，先通读注释）
2. `autograd.py`（第 8 章的反向传播引擎，最值得精读）
3. 然后按 Ch1 → Ch20 顺序读，每章 README 里有"概念→公式→代码对照表"
